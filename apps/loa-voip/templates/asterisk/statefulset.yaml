{{- $cluster := .Values.cluster | default dict -}}
{{- $hasFederation := hasKey ($cluster.apps | default dict) "loa-federation" -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-asterisk
spec:
  serviceName: {{ .Release.Name }}-asterisk
  replicas: {{ .Values.asterisk.replicaCount }}
  selector:
    matchLabels:
      app: asterisk
  template:
    metadata:
      labels:
        app: asterisk
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/asterisk/configmap.yaml") . | sha256sum }}
    spec:
      initContainers:
        - name: init-asterisk-lib
          image: {{ .Values.asterisk.image }}
          command: ['sh', '-c', 'cp -R /var/lib/asterisk/* /mnt/asterisk-lib/ 2>/dev/null || true']
          volumeMounts:
            - name: asterisk-lib
              mountPath: /mnt/asterisk-lib
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
        {{- if $hasFederation }}
        - name: init-ca-bundle
          image: cgr.dev/chainguard/stunnel:latest
          command:
            - "sh"
            - "-c"
            - "cat /certs/cas/ca-bundle.crt /certs/db-client/ca.crt > /certs/combined/ca-bundle.crt 2>/dev/null || cat /certs/cas/ca-bundle.crt > /certs/combined/ca-bundle.crt"
          volumeMounts:
            - name: cas
              mountPath: /certs/cas
              readOnly: true
            - name: db-client-certs
              mountPath: /certs/db-client
              readOnly: true
            - name: combined-ca-volume
              mountPath: /certs/combined
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
        {{- end }}
      containers:
        - name: asterisk
          image: {{ .Values.asterisk.image }}
          {{- if $hasFederation }}
          command: ["sh", "-c", "sleep 2 && exec /usr/sbin/asterisk -f -vvvddd"]
          {{- else }}
          command: ["/usr/sbin/asterisk", "-f", "-vvvddd"]
          {{- end }}
          ports:
            - name: sip-internal
              containerPort: {{ .Values.asterisk.ports.sip }}
              protocol: UDP
          volumeMounts:
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/pjsip.conf
              subPath: pjsip.conf
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/extensions.conf
              subPath: extensions.conf
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/confbridge.conf
              subPath: confbridge.conf
            {{- if $hasFederation }}
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/sorcery.conf
              subPath: sorcery.conf
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/extconfig.conf
              subPath: extconfig.conf
            - name: asterisk-config-volume
              mountPath: /etc/asterisk/res_odbc.conf
              subPath: res_odbc.conf
            - name: asterisk-config-volume
              mountPath: /etc/odbc.ini
              subPath: odbc.ini
            - name: asterisk-config-volume
              mountPath: /etc/odbcinst.ini
              subPath: odbcinst.ini
            - name: cas
              mountPath: /certs/cas
              readOnly: true
            {{- end }}
            - name: asterisk-run
              mountPath: /var/run/asterisk
            - name: asterisk-spool
              mountPath: /var/spool/asterisk
            - name: asterisk-log
              mountPath: /var/log/asterisk
            - name: asterisk-lib
              mountPath: /var/lib/asterisk
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
        {{- if $hasFederation }}
        - name: stunnel
          image: cgr.dev/chainguard/stunnel:latest
          command: ["stunnel", "/etc/stunnel/stunnel.conf"]
          volumeMounts:
            - name: asterisk-config-volume
              mountPath: /etc/stunnel/stunnel.conf
              subPath: stunnel.conf
              readOnly: true
            - name: db-client-certs
              mountPath: /certs/db-client
              readOnly: true
            - name: cas
              mountPath: /certs/cas
              readOnly: true
            - name: combined-ca-volume
              mountPath: /certs/combined
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
        {{- end }}
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: asterisk-config-volume
          configMap:
            name: asterisk-config
        - name: asterisk-run
          emptyDir: {}
        - name: asterisk-spool
          emptyDir: {}
        - name: asterisk-log
          emptyDir: {}
        - name: asterisk-lib
          emptyDir: {}
        {{- if $hasFederation }}
        - name: combined-ca-volume
          emptyDir: {}
        - name: cas
          configMap:
            name: trusted-ca-bundle
        - name: db-client-certs
          secret:
            secretName: pgedge-asterisk-client-cert
            defaultMode: 256
        {{- end }}

{{- if $hasFederation }}
---
apiVersion: db.loa.kevinpthorne.com/v1alpha1
kind: PrivateDatabase
metadata:
  name: asterisk
  namespace: {{ .Release.Namespace }}
spec:
  databaseName: asterisk
  userName: asterisk
  appNamespace: {{ .Release.Namespace }}
  federate: false
{{- end }}