replicaCount: 1

image:
  repository: jacobalberty/unifi
  pullPolicy: IfNotPresent
  tag: latest

mongodb:
  image:
    repository: mongo
    pullPolicy: IfNotPresent
    tag: "7.0"
  persistence:
    storageClass: longhorn
    size: 2Gi
  resources:
    limits:
      cpu: 500m
      memory: 350Mi
    requests:
      cpu: 100m
      memory: 256Mi

persistence:
  storageClass: longhorn
  size: 2Gi

timezone: "America/New_York"

service:
  type: LoadBalancer

podSecurityContext:
  runAsUser: 999
  runAsGroup: 999
  fsGroup: 999
