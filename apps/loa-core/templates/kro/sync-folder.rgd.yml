apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: syncfolders
spec:
  schema:
    apiVersion: fs.loa.kevinpthorne.com/v1alpha1
    kind: SyncFolder
    spec:
      path: string
      sourceCluster: "string | default=local"
      targetCluster: string
  resources:
    - id: filer-sync-deployment
      template:
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: "seaweedfs-sync-${schema.metadata.name}"
          namespace: seaweedfs-system
        spec:
          replicas: 1
          selector:
            matchLabels:
              app: seaweedfs-filer-sync
          template:
            metadata:
              labels:
                app: seaweedfs-filer-sync
            spec:
              containers:
              - name: filer-sync
                image: chrislusf/seaweedfs:4.0.401
                command:
                  - "weed"
                  - "filer.sync"
                args:
                  - "-a=seaweedfs-filer.seaweedfs.svc.cluster.${schema.spec.sourceCluster}:8888"
                  - "-b=seaweedfs-filer.seaweedfs.svc.cluster.${schema.spec.targetCluster}:8888"
                  - "-a.path=${schema.spec.path}"
                  - "-b.path=${schema.spec.path}"
