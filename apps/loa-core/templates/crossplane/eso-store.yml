apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: k8s-local-crossplane
  namespace: crossplane-system
spec:
  provider:
    kubernetes:
      server:
        url: "https://kubernetes.default.svc.cluster.local"
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          # This matches the SA we bound the Role to
          name: crossplane