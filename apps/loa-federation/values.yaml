argoNamespace: argo-cd
cluster: {}

pki:
  rootCAIssuer: app-pki-bootstrap-issuer  # currently implied to be ClusterIssuer
  certManagerNamespace: cert-manager

pgedge:
  instances: 1
  appName: main
  namespace: pgedge

keydb:
  instances: 0
  namespace: keydb

immich:
  namespace: immich

netbird:
  namespace: netbird
  controllerNamespace: netbird-controller