apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgedge-client-cert
  namespace: {{ .Values.pgedge.namespace }}
spec:
  secretName: pgedge-client-cert
  secretTemplate:
    labels:
      cnpg.io/reload: ""
  usages:
    - client auth
  commonName: pgedge
  issuerRef:
    name: pgedge-{{ .Values.pgedge.appName }}-client-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
  privateKey:
    rotationPolicy: Never