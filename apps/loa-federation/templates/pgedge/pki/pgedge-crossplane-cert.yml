apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: crossplane-client-cert
  namespace: crossplane-system  # TODO: this should be fed in via values.yaml
spec:
  secretName: crossplane-client-cert
  usages:
    - client auth
  commonName: crossplane
  issuerRef:
    name: pgedge-{{ .Values.pgedge.appName }}-client-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
  privateKey:
    rotationPolicy: Never