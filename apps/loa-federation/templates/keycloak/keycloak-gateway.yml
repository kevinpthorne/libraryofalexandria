apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak-gateway-app
  namespace: {{ .Values.argoNamespace }}
spec:
  project: default
  source:
    repoURL: https://github.com/kevinpthorne/libraryofalexandria.git
    path: pkgs/gateway-helm
    targetRevision: HEAD
    helm:
      values: |
        endpoints:
        - name: keycloak-app
          createGateway: false
          gatewayName: local-gateway
          gatewayNamespace: kube-system
          hostnames:
          - ident.{{ .Values.cluster.name }}.loa.internal
          - ident.kpt.link
          ports:
            - port: 443
              targetPort: 8443
              protocol: TLS
              tls:
                mode: Passthrough
              targetService: keycloak
              annotations:
                "external-dns.loa.internal/federate": "true"
  destination:
    server: https://kubernetes.default.svc
    namespace: keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    # pull from internet
    managedNamespaceMetadata:
      labels:
        zarf.dev/agent: "ignore"